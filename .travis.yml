
language: java
jdk: 
  - oraclejdk8

cache:
  directories:
  - $HOME/.m2

install: true

script:
  - cd APIGateway && ./mvnw clean install -DskipTests
  - cd ../ConfigServer && ./mvnw clean install -DskipTests